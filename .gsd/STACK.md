# Technology Stack

> Auto-generated by /map on 2026-04-12

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Python | 3.x | Primary runtime for all backend, ML, and DNS logic |

---

## Production Dependencies

| Package | Purpose |
|---------|---------|
| `flask` | REST API server + HTML template rendering |
| `dnslib` | DNS packet parsing/construction for the DNS proxy |
| `scikit-learn` | RandomForestClassifier (DGA), IsolationForest (tunneling) |
| `numpy` | Numerical arrays for ML feature vectors |
| `pandas` | Data manipulation for training pipelines |
| `joblib` | Serialization/deserialization of `.pkl` ML models |
| `requests` | HTTP client for external feed fetching |
| `google-generativeai` | Gemini AI integration for optional threat analysis |
| `openai` | OpenAI API integration (alternative AI analysis) |
| `datasets` | HuggingFace datasets library for training data ingestion |
| `sqlite3` | Built-in Python — local database for all query/alert storage |

---

## Development Dependencies

| Package | Purpose |
|---------|---------|
| `ruff` | Python linter (`.ruff_cache/` present, version 0.15.10) |

---

## Infrastructure

| Service | Provider | Purpose |
|---------|----------|---------|
| SQLite DB | Local file (`dns_filter.db`, 6.9 MB) | Persistent storage for DNS queries, alerts, blocklist, settings |
| ML Models | Local files (`.pkl`) | DGA model (107 MB Random Forest), Tunneling model (1.7 MB Isolation Forest) |
| Upstream DNS | Google (8.8.8.8) | Resolving clean/whitelisted domains |
| Threat Feeds | External URLs | Updating static blocklist via feed_updater daemon |

---

## Configuration

| Variable / Setting | Purpose | Location |
|-------------------|---------|----------|
| `BIND_IP` | DNS server bind address (default: `0.0.0.0`) | `dns_server.py` |
| `BIND_PORT` | DNS listen port (default: `5054`) | `dns_server.py` |
| `UPSTREAM_DNS` | Upstream DNS resolver (default: `8.8.8.8`) | `dns_server.py` |
| `SINKHOLE_IP` | IP returned for blocked domains (default: `0.0.0.0`) | `dns_server.py` |
| `DB_FILE` | SQLite database file path | `app.py`, `dns_server.py` |
| `dga_enabled` | Toggle DGA detection on/off | SQLite `system_settings` table |
| `tunnel_enabled` | Toggle tunneling detection on/off | SQLite `system_settings` table |
| `risk_threshold` | Score threshold for blocking (default: `80.0`) | SQLite `system_settings` table |
| Gemini API key | AI threat analysis | Not yet in `.env` — ⚠️ needs secrets management |
| OpenAI API key | Alternative AI analysis | Not yet in `.env` — ⚠️ needs secrets management |

---

## ML Models

| Model File | Algorithm | Size | Purpose |
|------------|-----------|------|---------|
| `dga_rf_model.pkl` | Random Forest Classifier | ~107 MB | Detect Domain Generation Algorithm (DGA) domains |
| `tunneling_iso_model.pkl` | Isolation Forest | ~1.7 MB | Detect DNS tunneling via anomaly scoring |

---

## Frontend Stack

| Technology | Purpose |
|-----------|---------|
| HTML5 (Jinja2 templates) | Dashboard page structure |
| Vanilla CSS | Liquid-glass dark theme styling |
| Vanilla JavaScript | Live chart polling, real-time data updates |
| Flask `render_template` | Server-side template rendering |

---

## Missing / Not Installed

| Item | Status | Impact |
|------|--------|--------|
| `requirements.txt` | ❌ Missing | Cannot reproduce environment — **HIGH RISK** |
| `.env` file | ❌ Missing | API keys are hardcoded or missing — **SECURITY RISK** |
| `pytest` / test runner | ❌ Missing | No automated test framework configured |
| Port 53 binding | ⚠️ Needs admin | Production DNS requires elevated privileges on Windows |
